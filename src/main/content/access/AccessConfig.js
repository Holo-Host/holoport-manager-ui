import Access from './Access';

export const AccessConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/access',
            component: Access
        }
    ]
};
